rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Allow uploads only for logged-in users
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
